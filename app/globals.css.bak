@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";

@custom-variant dark (&:is(.dark *));

/* Wyczesany HQ — Design System v1.
   Inspiracja: ClickUp 4.0 × Tasker accessibility.
   Jasny motyw, kremowe tlo. NIGDY dark mode.
   Layout: Rail (52px) + Sidebar (240px) + Content (fluid). */
:root {
  /* === BACKGROUND === */
  --background: #FBF8F3;
  --bg-base: #FBF8F3;
  --bg-surface: #FFFFFF;
  --bg-surface-hover: #F7F5F0;
  --bg-surface-active: #F0EDE6;
  --bg-elevated: #FFFFFF;
  --bg-sidebar: #EBE6DD;
  --bg-sidebar-hover: #E3DED4;
  --bg-muted: #F0EDE6;
  --background-soft: #F5EFE3;

  /* === TEXT === */
  --foreground: #1F1F2E;
  --text-primary: #1F1F2E;
  --text-secondary: #6B6B7B;
  --text-tertiary: #9B9BAB;
  --foreground-muted: #6B6B7B;

  /* === ACCENT / BRAND === */
  --accent: #6C5CE7;
  --accent-hover: #5A4BD6;
  --accent-light: #EDE9FC;
  --accent-subtle: #F5F3FF;
  --ds-accent: #6C5CE7;
  --ds-accent-hover: #5A4BD6;
  --ds-accent-light: #EDE9FC;
  --ds-accent-subtle: #F5F3FF;

  /* === TEXT extras === */
  --text-on-accent: #FFFFFF;

  /* === STATUS extras === */
  --status-info: #74B9FF;
  --status-info-light: #EBF5FF;

  /* === FONT === */
  --font-family: 'Nunito', -apple-system, BlinkMacSystemFont, sans-serif;

  /* === STATUS === */
  --status-success: #00B894;
  --status-success-light: #E6F9F3;
  --status-warning: #FDCB6E;
  --status-warning-light: #FFF8E6;
  --status-danger: #E17055;
  --status-danger-light: #FDF0ED;

  /* === BORDER === */
  --border-default: #E5E2DB;
  --border-strong: #D1CEC6;
  --border-subtle: #EDEBE5;
  --border-accent: #6C5CE7;
  --ink: #1F1F2E;

  /* === SHADOW (soft, nie offset) === */
  --shadow-sm: 0 1px 2px rgba(31,31,46,0.04);
  --shadow-md: 0 2px 8px rgba(31,31,46,0.06), 0 1px 2px rgba(31,31,46,0.04);
  --shadow-lg: 0 4px 16px rgba(31,31,46,0.08), 0 2px 4px rgba(31,31,46,0.04);
  --shadow-xl: 0 8px 32px rgba(31,31,46,0.10), 0 4px 8px rgba(31,31,46,0.06);

  /* === RADIUS === */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;

  /* === TRANSITION === */
  --transition-fast: 120ms ease;
  --transition-base: 200ms ease;

  /* Paleta 10 kolorow kontekstow — pelny hex */
  --ctx-fioletowy: #5B3DF5;
  --ctx-czerwony: #DC2626;
  --ctx-pomaranczowy: #F97316;
  --ctx-koralowy: #FF6B4A;
  --ctx-szary: #64748B;
  --ctx-zielony: #16A34A;
  --ctx-turkusowy: #0D9488;
  --ctx-rozowy: #DB2777;
  --ctx-niebieski: #2563EB;
  --ctx-zolty: #CA8A04;

  /* Pastelowe warianty — tla sekcji (miks z kremowym, oko przyjazne) */
  --ctx-fioletowy-soft: #E8E2FE;
  --ctx-czerwony-soft: #FCE4E4;
  --ctx-pomaranczowy-soft: #FEE7D0;
  --ctx-koralowy-soft: #FFE1D8;
  --ctx-szary-soft: #E6E9ED;
  --ctx-zielony-soft: #DDF3E2;
  --ctx-turkusowy-soft: #D5EEEA;
  --ctx-rozowy-soft: #FBDDEB;
  --ctx-niebieski-soft: #DCE7FB;
  --ctx-zolty-soft: #FBEFCF;

  /* Aliasy kontekstow startowych — zostaja dla kompatybilnosci */
  --ctx-salony: var(--ctx-fioletowy);
  --ctx-not-bad-stuff: var(--ctx-czerwony);
  --ctx-szkolenia: var(--ctx-pomaranczowy);
  --ctx-marka-osobista: var(--ctx-koralowy);

  /* === SHADCN OVERRIDES — zmapowane na DS v1 tokeny === */
  --card: #FFFFFF;
  --card-foreground: #1F1F2E;
  --popover: #FFFFFF;
  --popover-foreground: #1F1F2E;
  --primary: #6C5CE7;
  --primary-foreground: #FFFFFF;
  --secondary: #F0EDE6;
  --secondary-foreground: #1F1F2E;
  --muted: #F0EDE6;
  --muted-foreground: #6B6B7B;
  --accent: #6C5CE7;
  --accent-foreground: #FFFFFF;
  --destructive: #E17055;
  --destructive-foreground: #FFFFFF;
  --border: #E5E2DB;
  --input: #E5E2DB;
  --ring: #6C5CE7;
  --chart-1: #6C5CE7;
  --chart-2: #00B894;
  --chart-3: #FDCB6E;
  --chart-4: #E17055;
  --chart-5: #74B9FF;
  --radius: 0.625rem;
  --sidebar: #EBE6DD;
  --sidebar-foreground: #1F1F2E;
  --sidebar-primary: #6C5CE7;
  --sidebar-primary-foreground: #FFFFFF;
  --sidebar-accent: #EDE9FC;
  --sidebar-accent-foreground: #1F1F2E;
  --sidebar-border: #E5E2DB;
  --sidebar-ring: #6C5CE7;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-sans);
  --font-heading: var(--font-sans);
  --color-sidebar-ring: var(--sidebar-ring);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar: var(--sidebar);
  --color-chart-5: var(--chart-5);
  --color-chart-4: var(--chart-4);
  --color-chart-3: var(--chart-3);
  --color-chart-2: var(--chart-2);
  --color-chart-1: var(--chart-1);
  --color-ring: var(--ring);
  --color-input: var(--input);
  --color-border: var(--border);
  --color-destructive: var(--destructive);
  --color-accent-foreground: var(--accent-foreground);
  --color-accent: var(--accent);
  --color-muted-foreground: var(--muted-foreground);
  --color-muted: var(--muted);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-secondary: var(--secondary);
  --color-primary-foreground: var(--primary-foreground);
  --color-primary: var(--primary);
  --color-popover-foreground: var(--popover-foreground);
  --color-popover: var(--popover);
  --color-card-foreground: var(--card-foreground);
  --color-card: var(--card);
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) * 1.4);
  --radius-2xl: calc(var(--radius) * 1.8);
  --radius-3xl: calc(var(--radius) * 2.2);
  --radius-4xl: calc(var(--radius) * 2.6);
}

html {
  font-size: 15px;
}

body {
  line-height: 1.6;
  font-weight: 500;
  letter-spacing: -0.005em;
  -webkit-font-smoothing: antialiased;
}

h1, h2, h3, h4, h5, h6 {
  font-weight: 800;
  line-height: 1.15;
  letter-spacing: -0.02em;
}

h1 { font-size: 1.87rem; }   /* 28px */
h2 { font-size: 1.47rem; }   /* 22px */
h3 { font-size: 1.2rem; }    /* 18px */
h4 { font-size: 1.07rem; }   /* 16px */

/* Eyebrow — section label */
.eyebrow {
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--text-tertiary);
}

/* DS v1 Card */
.ds-card {
  background: var(--bg-surface);
  border: 1.5px solid var(--border-default);
  border-radius: var(--radius-lg);
  transition: all var(--transition-base);
}
.ds-card:hover {
  border-color: var(--border-strong);
  box-shadow: var(--shadow-md);
}

/* DS v1 Buttons */
.ds-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-weight: 700;
  border: none;
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
  font-family: inherit;
}
.ds-btn:active { transform: scale(0.97); }
.ds-btn-sm { padding: 6px 12px; font-size: 12px; border-radius: var(--radius-sm); }
.ds-btn-md { padding: 8px 16px; font-size: 13px; border-radius: var(--radius-sm); }
.ds-btn-primary { background: var(--accent); color: #fff; }
.ds-btn-primary:hover { background: var(--accent-hover); box-shadow: var(--shadow-sm); }
.ds-btn-secondary { background: var(--bg-surface); color: var(--text-primary); border: 1.5px solid var(--border-default); }
.ds-btn-secondary:hover { background: var(--bg-surface-hover); border-color: var(--border-strong); }
.ds-btn-ghost { background: transparent; color: var(--text-secondary); }
.ds-btn-ghost:hover { background: var(--bg-surface-hover); color: var(--text-primary); }

/* DS v1 Badge */
.ds-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 10px;
  font-size: 12px;
  font-weight: 700;
  border-radius: 9999px;
}

/* DS v1 Input */
.ds-input {
  width: 100%;
  padding: 9px 14px;
  font-family: inherit;
  font-size: 15px;
  color: var(--text-primary);
  background: var(--bg-surface);
  border: 1.5px solid var(--border-default);
  border-radius: var(--radius-sm);
  outline: none;
  transition: all var(--transition-fast);
}
.ds-input::placeholder { color: var(--text-tertiary); }
.ds-input:hover { border-color: var(--border-strong); }
.ds-input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-light); }

/* Backward compat — stare brutal classes mapuja na nowe */
.brutal-card { background: var(--bg-surface); border: 1.5px solid var(--border-default); border-radius: var(--radius-lg); }
.brutal-btn { display: inline-flex; align-items: center; gap: 8px; padding: 8px 16px; background: var(--bg-surface); border: 1.5px solid var(--border-default); border-radius: var(--radius-sm); font-weight: 700; cursor: pointer; }
.brutal-btn-primary { background: var(--accent); color: #fff; border-color: var(--accent); }

/* Stare .linear-app reguły usunięte — zastąpione przez t-* klasy w tasker-ds.css */

/* NIGDY dark mode — blok .dark celowo usuniety. */

@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
  }
  html {
    @apply font-sans;
  }
}

/* DS v1 — jedyne zrodlo prawdy dla komponentow Taskera */
@import "./tasker-ds.css";