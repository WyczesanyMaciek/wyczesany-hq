// Wyczesany HQ — /dev/design-system.
// Witryna designu: tokeny, typografia, paleta, cienie, karty, animacje.
// Kierunek: Neo-brutalist warm — grube bordery, twarde cienie offset,
// duze editorial naglowki, pastelowe tla per kontekst, spring physics.

import { DesignSystemClient } from "./design-system-client";

export default function DesignSystemPage() {
  return <DesignSystemClient />;
}
